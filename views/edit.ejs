<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
  <div class="w-full min-h-screen  bg-zinc-900 text-white p-10">
    <div class=" w-full flex justify-end">
        <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-4 inline-block" href="/logout">Logout</a>
    </div>
    <h5 class="mb-5">Edit your post</h5>
    <form action="/update/<%= post._id %>" method="post">
        <textarea name="content" class="resize-none bg-transparent border-2 border-zinc-800 rounded-md p-3 w-1/3 block " placeholder="what's on your mind ?">
            <%= post.content %>
        </textarea>
        <input type="submit" name="" id="" class="px-3 py-2 bg-yellow-500 block w-40 mt-2 text-black" value="Update post">
    </form>
   </div>
</body>
</html>