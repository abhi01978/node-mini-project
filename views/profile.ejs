<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
  <div class="w-full min-h-screen  bg-zinc-900 text-white p-10">
    <div class=" w-full flex justify-end">
        <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-4 inline-block" href="/logout">Logout</a>
    </div>
    <div class="flex items-start gap-3">
        <div class="w-10 h-10 rounded-md mt-2 "  >
            <img class="w-full h-full object-cover" src="/image/upload/<%= user.profilepic %>" alt="">
        </div>
        <h3 class="text-3xl"><span class="font-lite">Hello</span>, <%= user.name %></h3>

        
    </div>
    <a class="bg-yellow-500 rounded-md px-3 py-2 text-sm mb-4 inline-block mt-4" href="./profileupload">Change Profile picture</a>
    
    <h5 class="mb-5 mt-3">you can  create post</h5>
    <form action="/post" method="post">
        <textarea name="content" class="resize-none bg-transparent border-2 border-zinc-800 rounded-md p-3 w-1/3 block " placeholder="what's on your mind ?"></textarea>
        <input type="submit" name="" id="" class="px-3 py-2 bg-blue-500 block w-40 mt-2 text-sm" value="create new post">
    </form>
    <div class="posts mt-20">
        <h3 class="text-zinc-400">Your Posts.</h3>
        <div class="postcontainer mt-5">
            <% user.posts.reverse().forEach(function(post){ %>
                <div class="post w-1/3 p-4 mb-4  border-[1px] border-zinc-800 rounded-md  border-700">
                    <h4 class="text-blue-500 mb-3">@<%= user.username %></h4>
                    <p class="text-sm tracking-tight"><%= post.content %></p>
                     <small class="mt-54 inline-block"> <%= post.likes.length %> Like</small>
                    <div class="btn flex mt-1 gap-4">
                        <a class="text-blue-500" href="/like/<%= post._id %>">
                            <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                        </a>
                        <a class="text-zinc-600" href="/edit/<%= post._id %>">Edit</a>
                        <a class="text-white bg-red-500 h-30 w-40 text-center rounded-md" href="/delete/<%= post._id %>">deleteItem</a>
    
                    </div>
                </div>

         
           
            <% }) %>
    </div>
   
  </div>
    
</body>
</html>